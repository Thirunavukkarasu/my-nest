# Allow esbuild to run build scripts
# This is needed for vitest and other tools that depend on esbuild
# pnpm will still show a warning, but you can approve it with: pnpm approve-builds esbuild
enable-pre-post-scripts=true

